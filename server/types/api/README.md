### API Types
This package contains external API contracts for OpenImageRegistry. These types define the request and response structures exposed to clients.

### Stability Guarantee
Types in this package MUST remain stable across multiple versions of OpenImageRegistry to ensure backward compatibility with existing clients.