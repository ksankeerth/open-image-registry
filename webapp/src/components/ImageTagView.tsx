import React from 'react';

type ImageTagViewProps = {
  registery: string;
  namespace: string;
  image_repository: string;
  tag: string;
}
const ImageTagView = (props: ImageTagViewProps) => {
  return (<div>Image Tag View</div>)
}

export default ImageTagView;